# {{ .ProjectName }}

## 🌐 Languages

| | Language | README | Description |
|---|---|---|---|
| 🇺🇸 | English | [README.md](README.md) | Project scaffold & development guide |
| 🇨🇳 | 简体中文 | [README.zh-CN.md](README.zh-CN.md) | 项目脚手架与开发指南 |
| 🇹🇼 | 繁體中文 | [README.zh-TW.md](README.zh-TW.md) | 專案骨架與開發指南 |
| 🇩🇪 | Deutsch | [README.de.md](README.de.md) | Projektgerüst und Entwicklungshandbuch |
| 🇫🇷 | Français | [README.fr.md](README.fr.md) | Échafaudage de projet et guide de développement |
| 🇪🇸 | Español | [README.es.md](README.es.md) | Andamiaje del proyecto y guía de desarrollo |
| 🇯🇵 | 日本語 | [README.ja.md](README.ja.md) | プロジェクトの雛形と開発ガイド |
| 🇰🇷 | 한국어 | [README.ko.md](README.ko.md) | 프로젝트 스캐폴드 및 개발 가이드 |

---

## 📋 项目信息

| 字段 | 值 |
|---|---|
| 类型 | `{{ .ProjectType }}` |
| Agent | `{{ .AIAgent }}` |
| 后端 | `{{ .BackendType }}` |
| 前端 | `{{ .FrontendType }}` |
| 文档 | `{{ .DocsType }}` |

## 📝 描述

{{ .ProjectDescription }}

## `apa簡要說明`

- 开始实现循环：`apa prompt` -> 把输出贴给 agent -> 明确要求使用 `apa-loop` + `apa-implement`
- 先交互修改文档：`apa prompt --docs-only` -> 把输出贴给 agent -> 明确要求只使用 `apa-doc-review`
- Terminal loop wrapper：`bash scripts/apa-loop-setup.sh --max-iterations 30 --reviewer agent-self`
- 停止 wrapper：`bash scripts/apa-loop-cancel.sh`

## 📂 结构

{{- if or (eq .AIAgent "codex") (eq .AIAgent "universal") }}
- `AGENTS.md` — Codex agent 配置
- `PROMPT.md` — Prompt 工程指南
- `PLANS.md` — 实现计划
{{- if eq .AIAgent "universal" }}
- `CLAUDE.md` — Claude Code 配置
{{- end }}
- `docs/` — 从 `Phase 0` 开始的分阶段 PRD、SPEC、API、ARCHITECTURE、DEPLOYMENT
- `src/` — 源代码
- `tests/` — 测试套件（单元 + 集成）
- `tasks/queue/` — 每日任务队列
- `scripts/` — 工具脚本
- `agents/` — planner, coder, reviewer, test agents
- `skills/` — repo-local `apa-*` skills，用于指导 agent 工作
  - `apa-catalog` — 列出并说明所有可用 skills
  - `apa-debug` — 诊断并修复 bug
  - `apa-devops` — 部署、CI/CD、容器化
  - `apa-docs` — 按优先级分阶段生成或更新项目文档，从 Phase 0 开始，对齐 PRD/API/SPEC、测试、检查、下一阶段 gate 与完成报告
  - `apa-doc-review` — 与用户逐轮调整文档，每轮修改后停下等待反馈，文档批准前不得开始实现
  - `apa-feature` — 完整实现新功能
  - `apa-implement` — 读取既有文档后，以测试先行直接推进实现
  - `apa-integration` — 集成外部服务或 API
  - `apa-loop` — 分轮交付循环：读取状态、选 1-3 项任务、测试 → 实现 → 修正 → 更新状态 → 重复直到完成
    使用方式：先执行 `apa prompt`，再明确要求 agent 使用 `apa-loop` 与 `apa-implement`
  - `apa-review` — 审查 pull request 或 diff
  - `apa-claude-review` — Codex alias for code review using the same checklist as `apa-review`
{{- if eq .AIAgent "universal" }}
  - `apa-codex-review` — Claude alias for code review using the same checklist as `apa-review`
{{- end }}
  - `apa-tdd` — 检查 TDD 符合度、测试缺口与建议补测项
- `.codex/` — config.json, memory.md, cache/
{{- if eq .AIAgent "universal" }}
- `.claude/` — settings.json, memory.md, commands/
{{- end }}
{{- else }}
- `CLAUDE.md` — Claude Code 配置
- `AGENTS.md` — Agent 配置
- `docs/` — 从 `Phase 0` 开始的分阶段 PRD、SPEC、API、ARCHITECTURE、DEPLOYMENT
- `src/` — 源代码
- `tests/` — 测试套件（单元 + 集成）
- `tasks/queue/` — 每日任务队列
- `agents/` — planner, coder, reviewer, test agents
- `skills/` — repo-local `apa-*` skills，用于指导 agent 工作
  - `apa-catalog` — 列出并说明所有可用 skills
  - `apa-debug` — 诊断并修复 bug
  - `apa-devops` — 部署、CI/CD、容器化
  - `apa-docs` — 按优先级分阶段生成或更新项目文档，从 Phase 0 开始，对齐 PRD/API/SPEC、测试、检查、下一阶段 gate 与完成报告
  - `apa-doc-review` — 与用户逐轮调整文档，每轮修改后停下等待反馈，文档批准前不得开始实现
  - `apa-feature` — 完整实现新功能
  - `apa-implement` — 读取既有文档后，以测试先行直接推进实现
  - `apa-integration` — 集成外部服务或 API
  - `apa-loop` — 分轮交付循环：读取状态、选 1-3 项任务、测试 → 实现 → 修正 → 更新状态 → 重复直到完成
    使用方式：先执行 `apa prompt`，再明确要求 agent 使用 `apa-loop` 与 `apa-implement`
  - `apa-review` — 审查 pull request 或 diff
  - `apa-codex-review` — Claude alias for code review using the same checklist as `apa-review`
  - `apa-tdd` — 检查 TDD 符合度、测试缺口与建议补测项
- `.claude/` — settings.json, memory.md, commands/
{{- end }}

## 🔁 `apa-loop` 使用方式

- 如果要在实现前先反复修文档，可以先执行 `apa prompt --docs-only`，并明确要求 agent 只使用 `apa-doc-review`。
- Agent 主用法（Codex 与 Claude Code 通用）：先执行 `apa prompt`，再明确要求 agent 使用 `apa-loop` 与 `apa-implement`。
- 可选 terminal 包装（适用于有生成 hook 或 slash command 的环境，例如 Claude Code）：`bash scripts/apa-loop-setup.sh --max-iterations 30 --reviewer agent-self`
- 可选 slash command：`/apa-loop --max-iterations 30 --reviewer agent-self`
- 可选取消指令：`/cancel-apa-loop`
- Review policy: interactive per round. Ask which reviewer to use (`agent-self`, `apa-codex-review`, or `apa-claude-review`) before review.
- 两种情况都要在每一轮更新 `docs/IMPLEMENTATION_STATUS.md`，让下一轮从最新状态继续。

## 🚀 快速开始

1. 在 `docs/PRD.md` 中细化产品范围
2. 从 `skills/` 挑选合适的 skill 指导 agent 工作
3. 在 `src/` 中构建源代码，在 `tests/` 中构建测试
4. 运行 `make test` 验证（TDD：红灯 → 绿灯 → 重构）
5. 重复以上循环

## ⚙️ 开发命令

```bash
make test    # 运行所有测试
make lint    # 运行 linter
make build   # 构建项目
```

## 🛠️ 开发原则

- 每次变更都必须更新此 README 和相关的 `--help` 文本。
- README 和 `--help` 必须描述每个命令或功能产生的产物（输出）。
- 优先使用 `make test` 而非手动执行测试。
