# {{ .ProjectName }}

## 🌐 Langues

| | Langue | README | Description |
|---|---|---|---|
| 🇺🇸 | English | [README.md](README.md) | Project scaffold & development guide |
| 🇨🇳 | 简体中文 | [README.zh-CN.md](README.zh-CN.md) | 项目脚手架与开发指南 |
| 🇹🇼 | 繁體中文 | [README.zh-TW.md](README.zh-TW.md) | 專案骨架與開發指南 |
| 🇩🇪 | Deutsch | [README.de.md](README.de.md) | Projektgerüst und Entwicklungshandbuch |
| 🇫🇷 | Français | [README.fr.md](README.fr.md) | Échafaudage de projet et guide de développement |
| 🇪🇸 | Español | [README.es.md](README.es.md) | Andamiaje del proyecto y guía de desarrollo |
| 🇯🇵 | 日本語 | [README.ja.md](README.ja.md) | プロジェクトの雛形と開発ガイド |
| 🇰🇷 | 한국어 | [README.ko.md](README.ko.md) | 프로젝트 스캐폴드 및 개발 가이드 |

---

## 📋 Profil du projet

| Champ | Valeur |
|---|---|
| Type | `{{ .ProjectType }}` |
| Agent | `{{ .AIAgent }}` |
| Backend | `{{ .BackendType }}` |
| Frontend | `{{ .FrontendType }}` |
| Docs | `{{ .DocsType }}` |

## 📝 Description

{{ .ProjectDescription }}

## `apa簡要說明`

- Démarrer la boucle d'implémentation : `apa prompt --reviewer agent-self` -> coller la sortie dans l'agent -> demander `apa-loop` + `apa-implement`
- Réviser d'abord les documents : `apa prompt --docs-only` -> coller la sortie dans l'agent -> demander uniquement `apa-doc-review`
- Wrapper de boucle terminal : `bash scripts/apa-loop-setup.sh --max-iterations 30 --reviewer agent-self`
- Arrêter le wrapper : `bash scripts/apa-loop-cancel.sh`

## 📂 Structure

{{- if or (eq .AIAgent "codex") (eq .AIAgent "universal") }}
- `AGENTS.md` — Configuration de l'agent Codex
- `PROMPT.md` — Guide d'ingénierie des prompts
- `PLANS.md` — Plans d'implémentation
{{- if eq .AIAgent "universal" }}
- `CLAUDE.md` — Configuration Claude Code
{{- end }}
- `docs/` — PRD, SPEC, API, ARCHITECTURE et DEPLOYMENT structurés par phases à partir de `Phase 0`
- `src/` — Code source
- `tests/` — Suites de tests (unitaires + intégration)
- `tasks/queue/` — File de tâches quotidiennes
- `scripts/` — Scripts utilitaires
- `agents/` — planner, coder, reviewer, test agents
- `skills/` — skills repo-locaux `apa-*` pour guider l'agent
  - `apa-catalog` — lister et expliquer tous les skills disponibles
  - `apa-debug` — diagnostiquer et corriger les bugs
  - `apa-devops` — déploiement, CI/CD, conteneurisation
  - `apa-docs` — générer ou mettre à jour une documentation par priorité à partir de la Phase 0, avec PRD/API/SPEC alignés, tests, contrôles, gates de phase suivants et rapports de fin
  - `apa-doc-review` — réviser les documents avec l'utilisateur tour par tour, s'arrêter après chaque révision pour recueillir un retour, et ne pas lancer l'implémentation avant approbation
  - `apa-feature` — implémenter une nouvelle fonctionnalité de bout en bout
  - `apa-implement` — lire la documentation existante et piloter une implémentation test-first
  - `apa-integration` — intégrer des services externes ou des API
  - `apa-loop` — boucle de livraison par tours : lire l'état, choisir 1-3 tâches, tester → implémenter → corriger → mettre à jour l'état → répéter jusqu'à la fin
    Utilisation : lancer d'abord `apa prompt`, puis demander explicitement à l'agent d'utiliser `apa-loop` avec `apa-implement`
  - `apa-review` — revoir une pull request ou un diff
  - `apa-claude-review` — Codex alias for code review using the same checklist as `apa-review`
{{- if eq .AIAgent "universal" }}
  - `apa-codex-review` — Claude alias for code review using the same checklist as `apa-review`
{{- end }}
  - `apa-tdd` — auditer la discipline TDD, les trous de tests et les prochains tests recommandés
- `.codex/` — config.json, memory.md, cache/
{{- if eq .AIAgent "universal" }}
- `.claude/` — settings.json, memory.md, commands/
{{- end }}
{{- else }}
- `CLAUDE.md` — Configuration Claude Code
- `AGENTS.md` — Configuration des agents
- `docs/` — PRD, SPEC, API, ARCHITECTURE et DEPLOYMENT structurés par phases à partir de `Phase 0`
- `src/` — Code source
- `tests/` — Suites de tests (unitaires + intégration)
- `tasks/queue/` — File de tâches quotidiennes
- `agents/` — planner, coder, reviewer, test agents
- `skills/` — skills repo-locaux `apa-*` pour guider l'agent
  - `apa-catalog` — lister et expliquer tous les skills disponibles
  - `apa-debug` — diagnostiquer et corriger les bugs
  - `apa-devops` — déploiement, CI/CD, conteneurisation
  - `apa-docs` — générer ou mettre à jour une documentation par priorité à partir de la Phase 0, avec PRD/API/SPEC alignés, tests, contrôles, gates de phase suivants et rapports de fin
  - `apa-doc-review` — réviser les documents avec l'utilisateur tour par tour, s'arrêter après chaque révision pour recueillir un retour, et ne pas lancer l'implémentation avant approbation
  - `apa-feature` — implémenter une nouvelle fonctionnalité de bout en bout
  - `apa-implement` — lire la documentation existante et piloter une implémentation test-first
  - `apa-integration` — intégrer des services externes ou des API
  - `apa-loop` — boucle de livraison par tours : lire l'état, choisir 1-3 tâches, tester → implémenter → corriger → mettre à jour l'état → répéter jusqu'à la fin
    Utilisation : lancer d'abord `apa prompt`, puis demander explicitement à l'agent d'utiliser `apa-loop` avec `apa-implement`
  - `apa-review` — revoir une pull request ou un diff
  - `apa-codex-review` — Claude alias for code review using the same checklist as `apa-review`
  - `apa-tdd` — auditer la discipline TDD, les trous de tests et les prochains tests recommandés
- `.claude/` — settings.json, memory.md, commands/
{{- end }}

## 🔁 Utilisation de `apa-loop`

- Si tu veux d'abord itérer sur les documents avant l'implémentation, lance `apa prompt --docs-only` et demande à l'agent d'utiliser uniquement `apa-doc-review`.
- Flux principal via agent (commun à Codex et Claude Code) : lance `apa prompt --reviewer agent-self`, ou lance `apa prompt` puis choisis le reviewer en interactif, puis demande explicitement à l'agent d'utiliser `apa-loop` avec `apa-implement`.
- Wrapper terminal optionnel (pour les environnements qui exposent un hook ou une slash command générés, comme Claude Code) : `bash scripts/apa-loop-setup.sh --max-iterations 30 --reviewer agent-self`
- Slash command optionnelle : `/apa-loop --max-iterations 30 --reviewer agent-self`
- Commande d'annulation optionnelle : `/cancel-apa-loop`
- Review policy: indique le reviewer une seule fois via `--reviewer` ou dans la première consigne, puis réutilise-le aux tours suivants sauf si tu demandes explicitement un changement (`agent-self`, `apa-codex-review`, `apa-claude-review`).
- Dans les deux cas, mets à jour `docs/IMPLEMENTATION_STATUS.md` à chaque tour pour que la boucle suivante reparte du dernier état.

## 🚀 Démarrage rapide

1. Affiner la portée du produit dans `docs/PRD.md`
2. Choisir un skill dans `skills/` pour guider l'agent
3. Construire le code source dans `src/` avec les tests dans `tests/`
4. Exécuter `make test` pour vérifier (TDD : rouge → vert → refactoriser)
5. Répéter

## ⚙️ Commandes de développement

```bash
make test    # Exécuter tous les tests
make lint    # Exécuter le linter
make build   # Construire le projet
```

## 🛠️ Principes de développement

- Chaque modification doit garder ce README et le texte `--help` pertinent à jour.
- Le README et `--help` doivent décrire ce que chaque commande ou fonctionnalité produit (artefacts de sortie).
- Préférer `make test` à l'invocation manuelle des tests.
