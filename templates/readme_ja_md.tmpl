# {{ .ProjectName }}

## 🌐 言語

| | 言語 | README | 説明 |
|---|---|---|---|
| 🇺🇸 | English | [README.md](README.md) | Project scaffold & development guide |
| 🇨🇳 | 简体中文 | [README.zh-CN.md](README.zh-CN.md) | 项目脚手架与开发指南 |
| 🇹🇼 | 繁體中文 | [README.zh-TW.md](README.zh-TW.md) | 專案骨架與開發指南 |
| 🇩🇪 | Deutsch | [README.de.md](README.de.md) | Projektgerüst und Entwicklungshandbuch |
| 🇫🇷 | Français | [README.fr.md](README.fr.md) | Échafaudage de projet et guide de développement |
| 🇪🇸 | Español | [README.es.md](README.es.md) | Andamiaje del proyecto y guía de desarrollo |
| 🇯🇵 | 日本語 | [README.ja.md](README.ja.md) | プロジェクトの雛形と開発ガイド |
| 🇰🇷 | 한국어 | [README.ko.md](README.ko.md) | 프로젝트 스캐폴드 및 개발 가이드 |

---

## 📋 プロジェクト情報

| フィールド | 値 |
|---|---|
| タイプ | `{{ .ProjectType }}` |
| Agent | `{{ .AIAgent }}` |
| バックエンド | `{{ .BackendType }}` |
| フロントエンド | `{{ .FrontendType }}` |
| Docs | `{{ .DocsType }}` |

## 📝 説明

{{ .ProjectDescription }}

## `apa簡要說明`

- 実装ループを始める: `apa prompt --reviewer agent-self` -> 出力を agent に貼る -> `apa-loop` + `apa-implement` を使うよう指示
- 先にドキュメントを詰める: `apa prompt --docs-only` -> 出力を agent に貼る -> `apa-doc-review` のみ使うよう指示
- Terminal loop wrapper: `bash scripts/apa-loop-setup.sh --max-iterations 30 --reviewer agent-self`
- wrapper を止める: `bash scripts/apa-loop-cancel.sh`

## 📂 構造

{{- if or (eq .AIAgent "codex") (eq .AIAgent "universal") }}
- `AGENTS.md` — Codex エージェント設定
- `PROMPT.md` — プロンプトエンジニアリングガイド
- `PLANS.md` — 実装計画
{{- if eq .AIAgent "universal" }}
- `CLAUDE.md` — Claude Code 設定
{{- end }}
- `docs/` — `Phase 0` から始まるフェーズ型の PRD、SPEC、API、ARCHITECTURE、DEPLOYMENT
- `src/` — ソースコード
- `tests/` — テストスイート（ユニット + 統合）
- `tasks/queue/` — 日次タスクキュー
- `scripts/` — ユーティリティスクリプト
- `agents/` — planner, coder, reviewer, test agents
- `skills/` — agent を導くための repo ローカル `apa-*` skills
  - `apa-catalog` — 利用可能なすべての skills を一覧・説明
  - `apa-debug` — バグの診断と修正
  - `apa-devops` — デプロイ、CI/CD、コンテナ化
  - `apa-docs` — Phase 0 から始まる優先度ベースの PRD/API/SPEC・テスト・確認項目・次フェーズゲート・完了レポート付きドキュメントを生成または更新
  - `apa-doc-review` — ユーザーとラウンドごとに文書を詰め、各修正後に止まってフィードバックを待ち、文書承認前は実装を始めない
  - `apa-feature` — 新機能のエンドツーエンド実装
  - `apa-implement` — 既存ドキュメントを読んでテスト先行で実装を推進
  - `apa-integration` — 外部サービスや API の統合
  - `apa-loop` — ラウンド型デリバリーループ：状態を読み、1-3 個のタスクを選び、テスト → 実装 → 修正 → 状態更新 → 完了まで繰り返す
    使い方: まず `apa prompt` を実行し、その後 agent に `apa-loop` と `apa-implement` を使うよう明示する
  - `apa-review` — プルリクエストや差分のレビュー
  - `apa-claude-review` — Codex alias for code review using the same checklist as `apa-review`
{{- if eq .AIAgent "universal" }}
  - `apa-codex-review` — Claude alias for code review using the same checklist as `apa-review`
{{- end }}
  - `apa-tdd` — TDD 準拠、テスト不足、追加すべきテスト候補を監査する
- `.codex/` — config.json, memory.md, cache/
{{- if eq .AIAgent "universal" }}
- `.claude/` — settings.json, memory.md, commands/
{{- end }}
{{- else }}
- `CLAUDE.md` — Claude Code 設定
- `AGENTS.md` — エージェント設定
- `docs/` — `Phase 0` から始まるフェーズ型の PRD、SPEC、API、ARCHITECTURE、DEPLOYMENT
- `src/` — ソースコード
- `tests/` — テストスイート（ユニット + 統合）
- `tasks/queue/` — 日次タスクキュー
- `agents/` — planner, coder, reviewer, test agents
- `skills/` — agent を導くための repo ローカル `apa-*` skills
  - `apa-catalog` — 利用可能なすべての skills を一覧・説明
  - `apa-debug` — バグの診断と修正
  - `apa-devops` — デプロイ、CI/CD、コンテナ化
  - `apa-docs` — Phase 0 から始まる優先度ベースの PRD/API/SPEC・テスト・確認項目・次フェーズゲート・完了レポート付きドキュメントを生成または更新
  - `apa-doc-review` — ユーザーとラウンドごとに文書を詰め、各修正後に止まってフィードバックを待ち、文書承認前は実装を始めない
  - `apa-feature` — 新機能のエンドツーエンド実装
  - `apa-implement` — 既存ドキュメントを読んでテスト先行で実装を推進
  - `apa-integration` — 外部サービスや API の統合
  - `apa-loop` — ラウンド型デリバリーループ：状態を読み、1-3 個のタスクを選び、テスト → 実装 → 修正 → 状態更新 → 完了まで繰り返す
    使い方: まず `apa prompt` を実行し、その後 agent に `apa-loop` と `apa-implement` を使うよう明示する
  - `apa-review` — プルリクエストや差分のレビュー
  - `apa-codex-review` — Claude alias for code review using the same checklist as `apa-review`
  - `apa-tdd` — TDD 準拠、テスト不足、追加すべきテスト候補を監査する
- `.claude/` — settings.json, memory.md, commands/
{{- end }}

## 🔁 `apa-loop` の使い方

- 実装前に文書を往復で詰めたい場合は、先に `apa prompt --docs-only` を実行し、agent へ `apa-doc-review` のみを使うよう明示する。
- Agent の主運用（Codex と Claude Code 共通）: `apa prompt --reviewer agent-self` を実行するか、`apa prompt` 実行後に対話で reviewer を選び、そのあと agent に `apa-loop` と `apa-implement` を使うよう明示する
- 任意の terminal ラッパー（生成済み hook や slash command がある環境、たとえば Claude Code 向け）: `bash scripts/apa-loop-setup.sh --max-iterations 30 --reviewer agent-self`
- 任意の slash command: `/apa-loop --max-iterations 30 --reviewer agent-self`
- 任意のキャンセルコマンド: `/cancel-apa-loop`
- Review policy: `--reviewer` または最初の指示で reviewer を一度指定し、その後のラウンドでは再利用します。明示的に切り替える場合だけ変更します（`agent-self`、`apa-codex-review`、`apa-claude-review`）。
- どちらの場合も、次のループが最新状態から始まるよう各ラウンドで `docs/IMPLEMENTATION_STATUS.md` を更新します。

## 🚀 クイックスタート

1. `docs/PRD.md` でプロダクトのスコープを精緻化する
2. `skills/` から適切な skill を選んで agent を導く
3. `src/` でソースコードを、`tests/` でテストを構築する
4. `make test` で検証する（TDD：赤 → 緑 → リファクタリング）
5. 繰り返す

## ⚙️ 開発コマンド

```bash
make test    # 全テスト実行
make lint    # リンター実行
make build   # プロジェクトをビルド
```

## 🛠️ 開発原則

- すべての変更はこの README と関連する `--help` テキストを最新の状態に保つ必要があります。
- README と `--help` は各コマンドや機能が生成するもの（出力アーティファクト）を説明する必要があります。
- 手動テストよりも `make test` を優先する。
