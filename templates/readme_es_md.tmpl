# {{ .ProjectName }}

## 🌐 Idiomas

| | Idioma | README | Descripción |
|---|---|---|---|
| 🇺🇸 | English | [README.md](README.md) | Project scaffold & development guide |
| 🇨🇳 | 简体中文 | [README.zh-CN.md](README.zh-CN.md) | 项目脚手架与开发指南 |
| 🇹🇼 | 繁體中文 | [README.zh-TW.md](README.zh-TW.md) | 專案骨架與開發指南 |
| 🇩🇪 | Deutsch | [README.de.md](README.de.md) | Projektgerüst und Entwicklungshandbuch |
| 🇫🇷 | Français | [README.fr.md](README.fr.md) | Échafaudage de projet et guide de développement |
| 🇪🇸 | Español | [README.es.md](README.es.md) | Andamiaje del proyecto y guía de desarrollo |
| 🇯🇵 | 日本語 | [README.ja.md](README.ja.md) | プロジェクトの雛形と開発ガイド |
| 🇰🇷 | 한국어 | [README.ko.md](README.ko.md) | 프로젝트 스캐폴드 및 개발 가이드 |

---

## 📋 Perfil del proyecto

| Campo | Valor |
|---|---|
| Tipo | `{{ .ProjectType }}` |
| Agent | `{{ .AIAgent }}` |
| Backend | `{{ .BackendType }}` |
| Frontend | `{{ .FrontendType }}` |
| Docs | `{{ .DocsType }}` |

## 📝 Descripción

{{ .ProjectDescription }}

## `apa簡要說明`

- Iniciar el bucle de implementación: `apa prompt --reviewer agent-self` -> pega la salida en el agente -> pide `apa-loop` + `apa-implement`
- Revisar primero la documentación: `apa prompt --docs-only` -> pega la salida en el agente -> pide solo `apa-doc-review`
- Wrapper de bucle en terminal: `bash scripts/apa-loop-setup.sh --max-iterations 30 --reviewer agent-self`
- Detener el wrapper: `bash scripts/apa-loop-cancel.sh`

## 📂 Estructura

{{- if or (eq .AIAgent "codex") (eq .AIAgent "universal") }}
- `AGENTS.md` — Configuración del agente Codex
- `PROMPT.md` — Guía de ingeniería de prompts
- `PLANS.md` — Planes de implementación
{{- if eq .AIAgent "universal" }}
- `CLAUDE.md` — Configuración de Claude Code
{{- end }}
- `docs/` — PRD, SPEC, API, ARCHITECTURE y DEPLOYMENT por fases desde `Phase 0`
- `src/` — Código fuente
- `tests/` — Suites de pruebas (unitarias + integración)
- `tasks/queue/` — Cola de tareas diarias
- `scripts/` — Scripts de utilidad
- `agents/` — planner, coder, reviewer, test agents
- `skills/` — skills locales `apa-*` del repo para guiar al agente
  - `apa-catalog` — listar y explicar todos los skills disponibles
  - `apa-debug` — diagnosticar y corregir bugs
  - `apa-devops` — despliegue, CI/CD, contenedores
  - `apa-docs` — generar o actualizar documentación por prioridad desde Phase 0, con PRD/API/SPEC alineados, pruebas, verificaciones, gates de siguiente fase e informes de cierre
  - `apa-doc-review` — iterar los documentos con el usuario ronda a ronda, detenerse tras cada revisión y no iniciar implementación hasta que la documentación quede aprobada
  - `apa-feature` — implementar una nueva funcionalidad de extremo a extremo
  - `apa-implement` — leer documentación existente y avanzar en la implementación con test-first
  - `apa-integration` — integrar servicios externos o APIs
  - `apa-loop` — bucle de entrega por rondas: leer estado, elegir 1-3 tareas, probar → implementar → corregir → actualizar estado → repetir hasta terminar
    Uso: primero ejecuta `apa prompt` y luego pide explícitamente al agente que use `apa-loop` con `apa-implement`
  - `apa-review` — revisar un pull request o diff
  - `apa-claude-review` — Codex alias for code review using the same checklist as `apa-review`
{{- if eq .AIAgent "universal" }}
  - `apa-codex-review` — Claude alias for code review using the same checklist as `apa-review`
{{- end }}
  - `apa-tdd` — auditar disciplina TDD, huecos de prueba y siguientes tests recomendados
- `.codex/` — config.json, memory.md, cache/
{{- if eq .AIAgent "universal" }}
- `.claude/` — settings.json, memory.md, commands/
{{- end }}
{{- else }}
- `CLAUDE.md` — Configuración de Claude Code
- `AGENTS.md` — Configuración de agentes
- `docs/` — PRD, SPEC, API, ARCHITECTURE y DEPLOYMENT por fases desde `Phase 0`
- `src/` — Código fuente
- `tests/` — Suites de pruebas (unitarias + integración)
- `tasks/queue/` — Cola de tareas diarias
- `agents/` — planner, coder, reviewer, test agents
- `skills/` — skills locales `apa-*` del repo para guiar al agente
  - `apa-catalog` — listar y explicar todos los skills disponibles
  - `apa-debug` — diagnosticar y corregir bugs
  - `apa-devops` — despliegue, CI/CD, contenedores
  - `apa-docs` — generar o actualizar documentación por prioridad desde Phase 0, con PRD/API/SPEC alineados, pruebas, verificaciones, gates de siguiente fase e informes de cierre
  - `apa-doc-review` — iterar los documentos con el usuario ronda a ronda, detenerse tras cada revisión y no iniciar implementación hasta que la documentación quede aprobada
  - `apa-feature` — implementar una nueva funcionalidad de extremo a extremo
  - `apa-implement` — leer documentación existente y avanzar en la implementación con test-first
  - `apa-integration` — integrar servicios externos o APIs
  - `apa-loop` — bucle de entrega por rondas: leer estado, elegir 1-3 tareas, probar → implementar → corregir → actualizar estado → repetir hasta terminar
    Uso: primero ejecuta `apa prompt` y luego pide explícitamente al agente que use `apa-loop` con `apa-implement`
  - `apa-review` — revisar un pull request o diff
  - `apa-codex-review` — Claude alias for code review using the same checklist as `apa-review`
  - `apa-tdd` — auditar disciplina TDD, huecos de prueba y siguientes tests recomendados
- `.claude/` — settings.json, memory.md, commands/
{{- end }}

## 🔁 Uso de `apa-loop`

- Si quieres iterar primero sobre la documentación antes de implementar, ejecuta `apa prompt --docs-only` y pide al agente que use solo `apa-doc-review`.
- Flujo principal por agente (común para Codex y Claude Code): ejecuta `apa prompt --reviewer agent-self`, o ejecuta `apa prompt` y elige el reviewer de forma interactiva, y luego pide explícitamente al agente que use `apa-loop` con `apa-implement`
- Wrapper opcional en terminal (para entornos con hook o slash command generado, como Claude Code): `bash scripts/apa-loop-setup.sh --max-iterations 30 --reviewer agent-self`
- Slash command opcional: `/apa-loop --max-iterations 30 --reviewer agent-self`
- Comando opcional de cancelación: `/cancel-apa-loop`
- Review policy: especifica el reviewer una sola vez con `--reviewer` o en la primera instrucción y reutilízalo en las siguientes rondas; solo cámbialo si lo pides explícitamente (`agent-self`, `apa-codex-review`, `apa-claude-review`).
- En ambos casos, actualiza `docs/IMPLEMENTATION_STATUS.md` en cada ronda para que el siguiente bucle arranque desde el último estado.

## 🚀 Inicio rápido

1. Refinar el alcance del producto en `docs/PRD.md`
2. Elegir un skill de `skills/` para guiar al agente
3. Construir el código fuente en `src/` con pruebas en `tests/`
4. Ejecutar `make test` para verificar (TDD: rojo → verde → refactorizar)
5. Repetir

## ⚙️ Comandos de desarrollo

```bash
make test    # Ejecutar todas las pruebas
make lint    # Ejecutar linter
make build   # Construir el proyecto
```

## 🛠️ Principios de desarrollo

- Cada cambio debe mantener actualizado este README y el texto `--help` relevante.
- El README y `--help` deben describir qué produce cada comando o función (artefactos de salida).
- Preferir `make test` sobre la invocación manual de pruebas.
