# {{ .ProjectName }}

## 🌐 Sprachen

| | Sprache | README | Beschreibung |
|---|---|---|---|
| 🇺🇸 | English | [README.md](README.md) | Project scaffold & development guide |
| 🇨🇳 | 简体中文 | [README.zh-CN.md](README.zh-CN.md) | 项目脚手架与开发指南 |
| 🇹🇼 | 繁體中文 | [README.zh-TW.md](README.zh-TW.md) | 專案骨架與開發指南 |
| 🇩🇪 | Deutsch | [README.de.md](README.de.md) | Projektgerüst und Entwicklungshandbuch |
| 🇫🇷 | Français | [README.fr.md](README.fr.md) | Échafaudage de projet et guide de développement |
| 🇪🇸 | Español | [README.es.md](README.es.md) | Andamiaje del proyecto y guía de desarrollo |
| 🇯🇵 | 日本語 | [README.ja.md](README.ja.md) | プロジェクトの雛形と開発ガイド |
| 🇰🇷 | 한국어 | [README.ko.md](README.ko.md) | 프로젝트 스캐폴드 및 개발 가이드 |

---

## 📋 Projektprofil

| Feld | Wert |
|---|---|
| Typ | `{{ .ProjectType }}` |
| Agent | `{{ .AIAgent }}` |
| Backend | `{{ .BackendType }}` |
| Frontend | `{{ .FrontendType }}` |
| Docs | `{{ .DocsType }}` |

## 📝 Beschreibung

{{ .ProjectDescription }}

## `apa簡要說明`

- Implementierungs-Loop starten: `apa prompt` -> Ausgabe an den Agenten geben -> `apa-loop` + `apa-implement` anweisen
- Erst Dokumente iterativ schärfen: `apa prompt --docs-only` -> Ausgabe an den Agenten geben -> nur `apa-doc-review` anweisen
- Terminal-Loop-Wrapper: `bash scripts/apa-loop-setup.sh --max-iterations 30 --reviewer agent-self`
- Wrapper stoppen: `bash scripts/apa-loop-cancel.sh`

## 📂 Struktur

{{- if or (eq .AIAgent "codex") (eq .AIAgent "universal") }}
- `AGENTS.md` — Codex-Agent-Konfiguration
- `PROMPT.md` — Prompt-Engineering-Leitfaden
- `PLANS.md` — Implementierungspläne
{{- if eq .AIAgent "universal" }}
- `CLAUDE.md` — Claude Code-Konfiguration
{{- end }}
- `docs/` — phasenbasierte PRD-, SPEC-, API-, ARCHITECTURE- und DEPLOYMENT-Dokumente ab `Phase 0`
- `src/` — Quellcode
- `tests/` — Testsuiten (Unit + Integration)
- `tasks/queue/` — Tägliche Aufgabenwarteschlange
- `scripts/` — Hilfsskripte
- `agents/` — planner, coder, reviewer, test agents
- `skills/` — repo-lokale `apa-*` Skills zur Agent-Führung
  - `apa-catalog` — alle verfügbaren Skills auflisten und erklären
  - `apa-debug` — Fehler diagnostizieren und beheben
  - `apa-devops` — Deployment, CI/CD, Containerisierung
  - `apa-docs` — prioritätsbasierte Projektdokumentation ab Phase 0 mit abgestimmtem PRD/API/SPEC, Tests, Prüfpunkten, Next-Phase-Gates und Abschlussberichten erstellen oder aktualisieren
  - `apa-doc-review` — Dokumente gemeinsam mit dem Nutzer Runde für Runde schärfen, nach jeder Revision stoppen und vor Freigabe der Doku keine Implementierung starten
  - `apa-feature` — neues Feature von Anfang bis Ende implementieren
  - `apa-implement` — vorhandene Dokumentation lesen und test-first direkt umsetzen
  - `apa-integration` — externe Dienste oder APIs integrieren
  - `apa-loop` — rundenbasierte Delivery-Schleife: Status lesen, 1-3 Aufgaben wählen, testen → implementieren → korrigieren → Status aktualisieren → wiederholen bis fertig
    Verwendung: zuerst `apa prompt` ausführen und den Agenten dann explizit anweisen, `apa-loop` mit `apa-implement` zu verwenden
  - `apa-review` — Pull Request oder Diff überprüfen
  - `apa-claude-review` — Codex alias for code review using the same checklist as `apa-review`
{{- if eq .AIAgent "universal" }}
  - `apa-codex-review` — Claude alias for code review using the same checklist as `apa-review`
{{- end }}
  - `apa-tdd` — TDD-Disziplin, Testlücken und empfohlene nächste Tests prüfen
- `.codex/` — config.json, memory.md, cache/
{{- if eq .AIAgent "universal" }}
- `.claude/` — settings.json, memory.md, commands/
{{- end }}
{{- else }}
- `CLAUDE.md` — Claude Code-Konfiguration
- `AGENTS.md` — Agent-Konfiguration
- `docs/` — phasenbasierte PRD-, SPEC-, API-, ARCHITECTURE- und DEPLOYMENT-Dokumente ab `Phase 0`
- `src/` — Quellcode
- `tests/` — Testsuiten (Unit + Integration)
- `tasks/queue/` — Tägliche Aufgabenwarteschlange
- `agents/` — planner, coder, reviewer, test agents
- `skills/` — repo-lokale `apa-*` Skills zur Agent-Führung
  - `apa-catalog` — alle verfügbaren Skills auflisten und erklären
  - `apa-debug` — Fehler diagnostizieren und beheben
  - `apa-devops` — Deployment, CI/CD, Containerisierung
  - `apa-docs` — prioritätsbasierte Projektdokumentation ab Phase 0 mit abgestimmtem PRD/API/SPEC, Tests, Prüfpunkten, Next-Phase-Gates und Abschlussberichten erstellen oder aktualisieren
  - `apa-doc-review` — Dokumente gemeinsam mit dem Nutzer Runde für Runde schärfen, nach jeder Revision stoppen und vor Freigabe der Doku keine Implementierung starten
  - `apa-feature` — neues Feature von Anfang bis Ende implementieren
  - `apa-implement` — vorhandene Dokumentation lesen und test-first direkt umsetzen
  - `apa-integration` — externe Dienste oder APIs integrieren
  - `apa-loop` — rundenbasierte Delivery-Schleife: Status lesen, 1-3 Aufgaben wählen, testen → implementieren → korrigieren → Status aktualisieren → wiederholen bis fertig
    Verwendung: zuerst `apa prompt` ausführen und den Agenten dann explizit anweisen, `apa-loop` mit `apa-implement` zu verwenden
  - `apa-review` — Pull Request oder Diff überprüfen
  - `apa-codex-review` — Claude alias for code review using the same checklist as `apa-review`
  - `apa-tdd` — TDD-Disziplin, Testlücken und empfohlene nächste Tests prüfen
- `.claude/` — settings.json, memory.md, commands/
{{- end }}

## 🔁 `apa-loop` Verwendung

- Wenn du die Dokumente vor der Implementierung iterativ schärfen willst, starte mit `apa prompt --docs-only` und weise den Agenten an, nur `apa-doc-review` zu verwenden.
- Primärer Agent-Workflow (für Codex und Claude Code): `apa prompt` ausführen und den Agenten dann explizit anweisen, `apa-loop` mit `apa-implement` zu verwenden.
- Optionaler Terminal-Wrapper (für Umgebungen mit generiertem Hook oder Slash-Command, z. B. Claude Code): `bash scripts/apa-loop-setup.sh --max-iterations 30 --reviewer agent-self`
- Optionaler Slash-Command: `/apa-loop --max-iterations 30 --reviewer agent-self`
- Optionaler Abbruchbefehl: `/cancel-apa-loop`
- Review policy: interactive per round. Ask which reviewer to use (`agent-self`, `apa-codex-review`, or `apa-claude-review`) before review.
- In beiden Fällen `docs/IMPLEMENTATION_STATUS.md` in jeder Runde aktualisieren, damit die nächste Schleife mit dem neuesten Stand startet.

## 🚀 Schnellstart

1. Produktumfang in `docs/PRD.md` verfeinern
2. Einen passenden Skill aus `skills/` zur Agent-Führung auswählen
3. Quellcode in `src/` mit Tests in `tests/` erstellen
4. `make test` zur Verifikation ausführen (TDD: rot → grün → refaktorieren)
5. Wiederholen

## ⚙️ Entwicklungsbefehle

```bash
make test    # Alle Tests ausführen
make lint    # Linter ausführen
make build   # Projekt bauen
```

## 🛠️ Entwicklungsprinzipien

- Jede Änderung muss dieses README und den relevanten `--help`-Text aktuell halten.
- README und `--help` müssen beschreiben, was jeder Befehl oder jedes Feature produziert (Ausgabe-Artefakte).
- `make test` gegenüber manueller Testausführung bevorzugen.
